CREATE TABLE `member` (
	`M_SEQ` INT(11) NOT NULL AUTO_INCREMENT COMMENT '멤버 순번',
	`ALIAS_NM` VARCHAR(50) NULL DEFAULT NULL COMMENT '닉네임 명',
	`USER_NM` VARCHAR(50) NULL DEFAULT NULL COMMENT '사용자 이름',
	`JAEJIK_YN` VARCHAR(50) NULL DEFAULT NULL COMMENT '재직 재학 여부',
	`LOGIN_ID` VARCHAR(50) NULL DEFAULT NULL COMMENT '로그인 ID',
	`LOGIN_PW` VARCHAR(50) NULL DEFAULT NULL COMMENT '로그인 PW',
	`MNGR_YN` CHAR(1) NULL DEFAULT NULL COMMENT '관리자 여부',
	`USER_GB` VARCHAR(20) NULL DEFAULT NULL COMMENT '사용자 구분',
	`SOSOK_CD` VARCHAR(30) NULL DEFAULT NULL COMMENT '소속 코드',
	`DAEHAK_NM` VARCHAR(50) NULL DEFAULT NULL COMMENT '소속 대학',
	`USER_GB_NM` VARCHAR(50) NULL DEFAULT NULL COMMENT '사용자 구분 명',
	`GAEIN_NO` VARCHAR(50) NULL DEFAULT NULL COMMENT '학번 또는 개인번호',
	`UUID` VARCHAR(50) NULL DEFAULT NULL COMMENT 'UUID',
	`SOSOK_ID` VARCHAR(50) NULL DEFAULT NULL COMMENT '소속 ID (소속코드랑 다름)',
	`SOSOK_NM` VARCHAR(50) NULL DEFAULT NULL COMMENT '소속 명',
	`INPUT_DM` VARCHAR(19) NULL DEFAULT NULL COMMENT '등록 일자',
	PRIMARY KEY (`M_SEQ`),
	UNIQUE INDEX `인덱스 2` (`LOGIN_ID`)
)
COMMENT='회원'
COLLATE='utf8_general_ci'
ENGINE=MyISAM
AUTO_INCREMENT=1009980;
